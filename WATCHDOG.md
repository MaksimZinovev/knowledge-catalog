# Watchdog — knowledge-catalog

Advisor review priorities for this project:

- **PRs target the fork, never upstream.** Verify the base repo before any `gh pr create`.
- Plans live in `plans/` — check the current plan file before declaring a step unplanned or out of scope.
- Graph/visual changes: require real-browser verification (chrome-devtools) before done, and a shareable artifact for user review.
- API keys live in `C:\Users\maksi\.config\env`.
